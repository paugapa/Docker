# Docker
Una vez todo instalado desde un cmd en la carpeta utilizaremos estos comandos
1. docker-compose up -d (Importante dentro de la carpeta)
2. http://localhost:8080 (Abrir el navegador con esto)
